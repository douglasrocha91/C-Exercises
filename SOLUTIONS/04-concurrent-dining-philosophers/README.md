# Dining Philosophers

A solution to the classic concurrent dining philosophers problem, using mutexes and resource hierarchy (last philosopher reversed) to prevent deadlock.

## Building and Running
```bash
make
make run
```
